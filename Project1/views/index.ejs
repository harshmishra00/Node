<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full h-screen  bg-gray-900 p-6 gap-6">
        <div class="bg-gray-800 p-8 rounded-2xl shadow-2xl w-full max-w-md border border-gray-700 justify-self-center">
            <form action="/create" method="post" class="space-y-5">

                <input type="text" name="title" placeholder="Enter text" class="w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg
            focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none placeholder-gray-400" />

                <textarea placeholder="Enter message"
                    class="w-full px-4 py-2 h-32 bg-gray-700 text-white border border-gray-600 rounded-lg 
            resize-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none placeholder-gray-400" name="details"></textarea>

                <input type="submit" value="Submit" class="w-full bg-blue-600 text-white py-2 rounded-lg cursor-pointer
            hover:bg-blue-500 active:scale-95 transition" />

            </form>
        </div>

        <div class="tasks flex gap-5 mt-20 justify-self-center">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){%>
                    <div class="bg-gray-500 border border-white w-72  rounded-md p-3">
                        <h1 class="text-white text-3xl tracking-tight"><%=val%></h1>
                        <a href="#" class="text-blue-500">Read more</a>
                    </div>
                <% }) %>
            <%} else { %>
                <h3 class="text-gray-300">No tasks yet</h3>
            <% } %>

                            
                            


        </div>
    </div>


</body>

</html>