<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Styled Form + Tasks</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 min-h-screen flex items-center justify-center p-6">

    <div class="w-full max-w-4xl grid grid-cols-1 md:grid-cols-2 gap-6">


        <div class="bg-gray-800 p-6 rounded-2xl shadow-xl">
            <h2 class="text-xl font-semibold text-white mb-4">Add Task</h2>

            <form class="space-y-4" action="/create" method="post" >

                <input type="text" placeholder="Enter title" class="w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg
                    focus:ring-2 focus:ring-blue-500 outline-none" name="title">

                <textarea placeholder="Enter description" rows="5" class="w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg
                    focus:ring-2 focus:ring-blue-500 outline-none" name="details"></textarea>

                <input type="submit" value="Add Task" class="w-full py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg cursor-pointer
                    transition-all">
            </form>
        </div>


        <div class="bg-gray-800 p-6 rounded-2xl shadow-xl space-y-4">
            <h2 class="text-xl font-semibold text-white mb-4">Tasks</h2>

            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="bg-gray-700 p-4 rounded-xl text-white">
                        <h3 class="font-semibold text-lg"><%= val %></h3>
                        <a href="./file/<%= val %>" class="text-blue-500 ">Read more..</a><br>
                        <a href="/file/delete/<%= val %>" class="p-1 rounded-lg bg-red-500 ">Delete</a>
                    </div>
                <% }) %>
            <% } else{ %>
                <h3 class="text-slate-500">No tasks yet..</h3>
            <% } %>

        </div>

    </div>

</body>

</html>